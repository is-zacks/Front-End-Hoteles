import { View, Text } from 'react-native';

export default function RoomsScreen() {
  return (
    <View className="flex-1 justify-center items-center">
      <Text className="text-lg font-semibold">Pantalla de Habitaciones</Text>
    </View>
  );
}
